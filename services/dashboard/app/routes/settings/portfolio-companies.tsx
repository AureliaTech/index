import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/settings/portfolio-companies')({
  component: () => <div>Portfolio Companies – coming soon.</div>,
}) 